"use strict";

function layoutRoughEdges(graph) {
  // TODO recursively get all rough edges in this graph

  // TODO get the coordinates of their start and end positions

  // TODO get all obstacles in this diagram

  // TODO lay out the rough edges

  // TODO draw them? maybe in a different method

  console.log(graph.roughEdges);
  return graph;
}

module.exports = { layoutRoughEdges };
